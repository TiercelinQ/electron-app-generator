---
name: electron-trace-feature
description: Analyze code, architecture, data flow, or behavior in an Electron app generated by this framework. Use when the user asks to understand, trace, audit, or investigate how something works across the model / controller / preload / view layers.
model: sonnet
---

# /electron-trace-feature — Analyze a generated Electron app

## Role
Analyst — trace and explain how something works, without changing it.

## Goal
Give the user an accurate, layered understanding of a feature or behavior, with exact code references.

## Deliverable
A structured report (in the user's language) with `file:line` references. Optionally `docs/specs/analyse-[topic].md` if the user wants it persisted.

---

> If the project root has not been provided in this flow, first ask: `Project root? (folder path)`.

## Steps

1. **Identify scope** — a single file, an entity (model+controller+view), a cross-cutting pattern (theming, error handling, IPC, security), or the whole app.

2. **Ask when ambiguous** — if the request has several valid readings, use `AskUserQuestion` before diving in. See "When to ask" below.

3. **Read the relevant sources** by layer:
   - Contract & specs: `docs/specs/04-architect.md` (structure of truth), other `docs/specs/*`.
   - Shared: `src/shared/config.ts`, `src/shared/types.ts` (DTOs + `WindowApi`), `src/shared/ipc-channels.ts`.
   - Main entry / security: `src/main/index.ts`.
   - Models: `src/main/models/[entity].model.ts`, `models/errors.ts`, `preferences.model.ts`, `models/sf-cli.ts` (if the Salesforce integration is on: runner + typed helpers; `sf-cli-reference/INDEX.md` is the command/flag reference, consulted on demand).
   - Controllers: `src/main/controllers/[entity].controller.ts`, `controllers/index.ts`.
   - Preload: `src/preload/index.ts`.
   - Views: `src/renderer/src/App.tsx`, `views/[Entity]View.tsx`, `views/layout/`, `views/ToastManager.tsx`, `hooks/`.
   - Styles: `src/renderer/src/styles/tokens.css`, `styles.css`.
   - Binding refs (read on demand, not auto-imported): `design-system.md`, `layout.md` — for design-system/layout deviations.

4. **Trace the flow** following the unidirectional path: view (`window.api.x()`) → preload (`ipcRenderer.invoke(channel)`) → controller (`ipcMain.handle`, validation) → model (logic, data). For an `sf` feature: `OrgView` → `window.api.sfOrg*` → preload → `org.controller.ts` → `SfCli.*` → `cross-spawn` → `sf --json` → parsed envelope → `IpcResult`. For errors: model throws → controller returns `IpcResult` → view toasts. For theming: token → `tokens.css` → `var(--token)` in `styles.css`. Cross-reference every IPC channel against `ipc-channels.ts`.

5. **Report** (in the user's language):
   - What the code does (behavior).
   - How it connects across layers (dependencies, data flow, IPC channels).
   - Any inconsistency vs the contract or the rules (`rules/*`), with the rule named — including security (`@rules/security.md`).
   - `file:line` references.
   - **Flag any unverified assumption explicitly** — do not fabricate a runtime behavior you could not read.

## When to ask the user

Use `AskUserQuestion` when:
- **Multiple valid scopes** — "analyze the settings tab" could mean the view, the controller, the preferences model, or all.
- **Multiple readings** — "how does saving work" — which entity? the IPC round-trip? the SQLite write? the validation?
- **Depth vs breadth** — a deep audit of one entity vs a survey of several.
- **Output format** — short summary, structured report, or actionable findings list.

Offer 2–4 concrete options, recommended one first. Do not ask when the scope is obvious or you are just confirming a fact.

## Anti-patterns — what NOT to do
- **Do not** modify code during an analysis. This skill is read-only; if a fix is wanted, hand off to `/electron-fix-issue` or `/electron-add-feature`.
- **Do not** report a behavior you inferred but did not read — mark it as an assumption and offer to verify.
- **Do not** describe the layers in the abstract — anchor every claim to a `file:line`.
- **Do not** silently pick one interpretation when several exist — ask.

## When the user asks something adjacent
- **"And fix it"** → switch to `/electron-fix-issue` (bug) or `/electron-add-feature` (change), carrying the analysis findings.
- **"Is this secure / well-architected?"** → compare against `@rules/security.md`, `docs/specs/04-architect.md`, and `@rules/mvc.md`; report deviations, do not refactor (that is `/electron-refactor-code`, on request).
